import DeleteIcon from './DeleteIcon';

export default {
    title: "COMPONENTS/Atoms/DeleteIcon",
    component: DeleteIcon
};

const Template = (args) => <DeleteIcon {...args} />;

export const Default = Template.bind();