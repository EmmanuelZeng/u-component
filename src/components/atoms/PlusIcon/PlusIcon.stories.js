import PlusIcon from './PlusIcon';

export default {
    title: "COMPONENTS/Atoms/PlusIcon",
    component: PlusIcon
};

const Template = (args) => <PlusIcon {...args} />;

export const Default = Template.bind();