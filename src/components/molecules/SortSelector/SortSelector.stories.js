import React from "react";
import SortSelector from "./SortSelector";

export default {
  title: "COMPONENTS/molecules/SortSelector",
  component: SortSelector,
};

const Template = (args) => <SortSelector {...args} />;

export const Default = Template.bind({});
Default.args = {};
