import React from "react";
import SearchFilterCombo from "./SearchFilterCombo";

export default {
  title: "COMPONENTS/molecules/SearchFilterCombo",
  component: SearchFilterCombo,
};

const Template = (args) => <SearchFilterCombo {...args} />;

export const Default = Template.bind({});
Default.args = {};
