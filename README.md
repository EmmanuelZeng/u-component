# Component repository for DGI web platforms

This project was created with love by @sbsdevit

## Available Scripts

In the project directory, you can run:

### `npm run storybook`

Run the storybook interface \
Open [http://localhost:6006/](http://localhost:6006) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

### `npm run build-lib`

Builds the app for production to the `dist` folder.\
It correctly bundles React component in production mode and optimizes the build for the best performance.

## Essential git command

You can switch from one branch to another with this command👇🏾

### `git checkout <branch_name>`

You push the code to particular branch👇🏾

### `git push origin <branch_name>`

You push to particular branch👇🏾

### `git pull origin <branch_name>`

### Deployment

### `npm publish`

Allow to publish in github package registry
